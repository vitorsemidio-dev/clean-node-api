{
  "name": "clean-node-api",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "jest --watch --silent --verbose --colors --noStackTrace",
    "test:staged": "jest --passWithNotTests --findRelatedTests --silent --noStackTrace",
    "test:ci": "jest --coverage --noStackTrace --silent"
  },
  "standard": {
    "env": ["jest"]
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "husky": "^4.2.3",
    "jest": "^26.6.3",
    "lint-staged": "^10.5.4",
    "standard": "^16.0.3"
  }
}
